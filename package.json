{
  "name": "inventory-app",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix server && npm ci --prefix web && npm run build --prefix web",
    "start": "NODE_ENV=production node server/index.js",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev --prefix server\" \"npm run dev --prefix web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}